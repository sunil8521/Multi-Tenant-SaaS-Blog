# Example .env for server (rename to .env in development; DO NOT commit real secrets)
NODE_ENV=development
PORT=3000

# BetterAuth secret key (replace with a secure random string)
BETTER_AUTH_SECRET=

# URL of the frontend or auth callback
BETTER_AUTH_URL=

EMAIL_USER=you@example.com
EMAIL_PASSWORD="your-email-password-or-app-specific-password"

# Redis connection string
REDIS_URL=


S3_ACCESS_KEY=
S3_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=
# NOTES:
# - Replace placeholders with real values in a secure environment.
# - Keep secrets out of version control; use secret management for production.